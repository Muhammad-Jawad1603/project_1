version: 0.2

phases:
  install:
    commands:
      - sudo apt-get update
      - sudo apt-get install -y docker
  pre_build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region eu-west-1 | docker login --username AWS --password-stdin 099199746132.dkr.ecr.eu-west-1.amazonaws.com
      - REPO_URI=099199746132.dkr.ecr.eu-west-1.amazonaws.com/demo_repo
      - IMAGE_TAG=$(date +%Y%m%d%H%M%S)
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - echo "Building the Docker image..."
      - docker build -t $REPO_URI:$IMAGE_TAG .
      - docker tag $REPO_URI:$IMAGE_TAG $REPO_URI:latest
  post_build:
    commands:
      - echo "Pushing the Docker image to Amazon ECR..."
      - docker push $REPO_URI:$IMAGE_TAG
      - docker push $REPO_URI:latest
      - echo "Writing image definition file..."
      - printf '[{"name":"node-app","imageUri":"%s"}]' $REPO_URI:latest > imagedefinitions.json
artifacts:
  files:
    - imagedefinitions.json
